import { StyleSheet } from "react-native";

export const initialStyles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundImage: '../../assets/images/fundo_initial.svg',
    },
    
});